[General]
# Language setting: en, ru, fr, it, de, zh, or auto
# auto = detect from system locale
# en = English, ru = Russian, fr = French, it = Italian, de = German, zh = Chinese (Simplified)
language = auto

[Arduino]
# Default Arduino port (leave empty for auto-detection)
port =

[Origin]
# Enable Origin integration by default
enabled = false

[Files]
# Measurements folder (leave empty for default Documents/TemperatureMonitor)
folder =
